'use client'

import { Booking } from "@prisma/client";

interface BoookingsListProps{
    booking: Booking
}

const BoookingsList = ({booking}: BoookingsListProps) => {
    return ( <>
    </> );
}
 
export default BoookingsList;